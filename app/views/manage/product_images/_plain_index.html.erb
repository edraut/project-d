<% product = plain_index %>
<% product_images = product.product_images %>
<% for product_image in product_images %>
	<%= render :partial => "manage/product_images/element_container", :object => product_image, :locals => {:row_end => ((product_images.index(product_image) % 2) == 1) } %>
<% end %>
