name:
<%= @product.name %><br/>
decription:
<%= @product.decription %><br/>

<%= link_to 'edit', edit_product_url(@product) %> | 
<%= link_to 'delete', product_url(@product), :method => :delete, :confirm => 'Are you sure you want to delete this product?' %>
